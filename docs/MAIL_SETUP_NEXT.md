# Next setup step

The repository now contains the ORENZA SMTP mail transport. To make it actually deliver mail, provision the mail server and DNS first, then add the production SMTP environment variables to the ORENZA deployment.

Requested operational recipient: `Supportdeveloperer@gmail.com`.

Do not add the Gmail address as a server credential. It is a recipient only.
