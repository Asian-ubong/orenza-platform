import Link from 'next/link';
import { Activity, AlertTriangle, Bot, ClipboardCheck, LockKeyhole, ShieldCheck, Users, WalletCards } from 'lucide-react';
const cards=[['USERS','Manage tester and platform accounts','/admin/users',Users],['KYC & AML','Review cases and compliance signals','/admin/kyc-and-aml',ShieldCheck],['SANDBOX FUNDING','Review demo funding requests','/admin/sandbox-funding',WalletCards],['APPROVAL CENTER','Owner-gated critical actions','/admin/payout-approvals',ClipboardCheck],['AI ACTION QUEUE','Review AI recommendations','/admin/ai-action-queue',Bot],['RISK & FRAUD','Monitor risk events and controls','/admin/risk-and-fraud',AlertTriangle],['SYSTEM HEALTH','Inspect incidents and providers','/admin/system-health',Activity],['AUDIT LOG','Immutable administrative history','/admin/audit-log',LockKeyhole]] as const;
export default function AdminPage(){return <><header className="pageHead"><div><p className="eyebrow">ORENZA OWNER CONTROL CENTER</p><h1>Command Center</h1><p className="muted">One privileged workspace for users, compliance, sandbox funding, trading operations, AI actions, risk, infrastructure and approvals.</p></div></header><div className="warning"><ShieldCheck size={18}/><span><b>Safety boundary:</b> real-money payments, withdrawals, transfers, live trading, KYC approval and other high-impact actions remain explicitly approval-gated. Tester access never bypasses these controls.</span></div><div className="metricGrid">{cards.map(([name,desc,href,Icon])=><Link className="card" href={href} key={name} style={{textDecoration:'none',color:'inherit'}}><div className="metricIcon"><Icon size={18}/></div><span className="label">{name}</span><strong className="metricValue">Open workspace</strong><small>{desc}</small></Link>)}</div><section className="section"><div className="card"><p className="eyebrow">RUNTIME SAFETY</p><h2>Sandbox-first operating posture</h2><p className="muted">Deriv market data and demo operations can be monitored here. Real-money runtime flags remain hard-disabled until separately authorized and enabled through controlled server-side policy.</p><div style={{display:'flex',gap:12,flexWrap:'wrap',marginTop:16}}><span className="badge good">SANDBOX MODE</span><span className="badge danger">REAL PAYMENTS OFF</span><span className="badge danger">REAL TRADING OFF</span><span className="badge danger">REAL PAYOUTS OFF</span></div></div></section></>}
