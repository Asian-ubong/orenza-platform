'use client';
import {useEffect,useState} from 'react';
import {useRouter} from 'next/navigation';
import {CheckCircle2,ShieldCheck} from 'lucide-react';
import {getSupabaseBrowser} from '../../lib/supabase-browser';

export default function KycPage(){const router=useRouter();const[status,setStatus]=useState('NOT_STARTED');const[caseId,setCaseId]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);
useEffect(()=>{(async()=>{const s=getSupabaseBrowser();const{data:{user}}=await s.auth.getUser();if(!user){router.replace('/login');return}const{data}=await s.rpc('get_drive_invest_kyc_status');if(data?.[0]){setCaseId(data[0].case_id);setStatus(data[0].status)}})()},[router]);
async function start(){setBusy(true);const s=getSupabaseBrowser();const{data,error}=await s.rpc('start_drive_invest_kyc',{p_verification_level:'STANDARD'});if(error)setMsg(error.message);else{setCaseId(data.id);setStatus(data.status);setMsg('KYC case created. Complete identity evidence before submission.')}setBusy(false)}
return <main style={{minHeight:'100vh',background:'#FAF9F6',padding:30,color:'#0B192B'}}><div style={{maxWidth:760,margin:'auto'}}><p className="eyebrow">ORENZA · KYC</p><h1 style={{color:'#2A402D'}}>Identity verification</h1><p className="muted">KYC is the gate between demo testing and any separately authorized real-account access. AI can assist with evidence and risk signals; an authorized human makes the final decision.</p><section className="card" style={{marginTop:18}}><div style={{display:'flex',justifyContent:'space-between'}}><strong>Status: {status}</strong><ShieldCheck size={19}/></div>{!caseId&&<button className="btn" onClick={start} disabled={busy} style={{marginTop:16}}>Start KYC</button>}{caseId&&<div style={{marginTop:16}}><div className="notice"><CheckCircle2 size={18}/><div><strong>KYC case opened</strong><span>Case ID is secured server-side. The next step is identity/profile evidence; no approval is granted automatically.</span></div></div><p className="muted" style={{marginTop:12}}>KYC evidence upload/submission remains the next implementation step for this account. Real-account opening and the 5,000 promo remain locked until the authorized review gates are satisfied.</p></div>}{msg&&<p className="muted">{msg}</p>}</section><div style={{display:'flex',gap:10,marginTop:14,flexWrap:'wrap'}}><button className="btn secondary" onClick={()=>router.push('/mt5')}>Back to MT5</button><button className="btn secondary" onClick={()=>router.push('/home')}>Main app</button></div></div></main>}
